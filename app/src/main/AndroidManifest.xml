<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.appoinkments">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activity_RecyclerAppointments"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Activity_NewLoad"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".Activity_ShowLoad"
            android:label="@string/app_name">
        </activity>
    
        <activity
            android:name=".Activity_RecyclerFarmers"
            android:label="@string/app_name">
        </activity>
    
        <activity
            android:name=".Activity_RecyclerLoads"
            android:label="@string/app_name">
        </activity>
    
        <receiver android:name=".AlarmReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.DATE_CHANGED"></action>
            </intent-filter>
        </receiver>
    </application>

</manifest>